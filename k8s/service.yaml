apiVersion: v1
kind: Service
metadata:
  name: atlas-core
  namespace: atlas-production
  labels:
    app: atlas-core
spec:
  type: ClusterIP
  selector:
    app: atlas-core
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
