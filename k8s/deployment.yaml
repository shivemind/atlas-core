apiVersion: apps/v1
kind: Deployment
metadata:
  name: atlas-core
  namespace: atlas-production
  labels:
    app: atlas-core
spec:
  replicas: 1
  selector:
    matchLabels:
      app: atlas-core
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: atlas-core
    spec:
      containers:
        - name: atlas-core
          image: PLACEHOLDER_ECR_REGISTRY/atlas-core:latest
          ports:
            - containerPort: 3000
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
      terminationGracePeriodSeconds: 30
